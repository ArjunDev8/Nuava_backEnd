import StudentResolvers from "./Student";
import CoachResolvers from "./Coach";

export const allResolvers = [StudentResolvers, CoachResolvers];

export default {
  StudentResolvers,
  CoachResolvers,
};
